import { parseStringPromise } from 'npm:xml2js';

export async function render({
  dev = false,
  write = false,
  mode = MODE.YOUTUBE,
  randomPicks = 0,
  useEmbedUrls = false,
  buildNewpipeSubs = false
} = {}) {
  
};